{
    "title": "Tomba Clearbit-Combined Actor Input",
    "description": "Input schema for the Tomba Clearbit-Combined Actor that enriches company information using the Tomba API.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "tombaApiKey": {
            "title": "Tomba API Key",
            "type": "string",
            "description": "Your Tomba API key (ta_xxxx). Get it from https://app.tomba.io/api",
            "editor": "textfield",
            "isSecret": true,
            "example": "ta_xxxxxxxxxxxxxxxxxxxx"
        },
        "tombaApiSecret": {
            "title": "Tomba Secret Key",
            "type": "string",
            "description": "Your Tomba secret key (ts_xxxx). Get it from https://app.tomba.io/api",
            "editor": "textfield",
            "isSecret": true,
            "example": "ts_xxxxxxxxxxxxxxxxxxxx"
        },
        "emails": {
            "title": "Emails to Enrich",
            "type": "array",
            "description": "Array of email addresses to enrich with company information.",
            "editor": "stringList",
            "items": {
                "type": "string"
            },
            "example": ["user1@example.com", "user2@example.com", "user3@example.com"]
        },
        "maxResults": {
            "title": "Maximum Results",
            "type": "integer",
            "description": "Maximum number of companies to enrich. Default is 50.",
            "editor": "number",
            "minimum": 1,
            "maximum": 1000,
            "default": 50,
            "example": 50
        }
    },
    "required": ["tombaApiKey", "tombaApiSecret", "emails"]
}
